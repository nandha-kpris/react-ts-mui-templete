import React, { useState } from 'react';
import { Typography } from '@mui/material';

function Home() {
  console.log('home page');

  return (
    <div
      style={{
        color: 'red',
        fontSize: '25px',
        marginLeft: '200px',
      }}
    >
      home
    </div>
  );
}

export default Home;
